#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Return: Always (0) Success
 */
int main(void)
{
	char i = 'a';
	while (i <= 'z')

	{
		putchar ("%s", i);
			i++;
	}
	return (0);
}
