#include "main.h"
/**
 * print_last_digit - function name
 * @i: variable
 * Return: Always (0) Success
 */
int print_last_digit(int i)
{
	
	i = i % 10;
	if (i != 0)
	{
	return (i);
	}
	else
	{
		return (0);
	}
}
