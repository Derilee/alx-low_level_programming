#include "main.h"
/**
 * print_last_digit - function name
 * @i: variable
 * Return: Always (0) Success
 */
int print_last_digit(int i)
{
	int n = i % 10;
	{
		_putchar(n);
		return (i);
	}
	return (0);
}
