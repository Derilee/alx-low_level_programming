#include "main.h"
/**
 * _islower - Entry point
 *c - Entry point
 * Return: ALways(0) Success
 */
int _islower(int c)
{
	if (_islower(c))
	{
		return (1);
	}
		else
		{
			return (0);
		}
		_putchar('\n');
}
